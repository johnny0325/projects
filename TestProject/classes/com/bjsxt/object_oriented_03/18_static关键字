
1.在类中，用static声明的成员变量为静态成员变量，它为该类的公用变量，在第一次使用时被初
始化，对于该类的所有对象来说，static成员变量只有一份。
2.用static声明的方法为静态方法，在调用该方法时，不会装对象的引用传递给它，所以在static
方法中不可访问非static的成员。
3.静态方法不再是针对于某个对象调用，所以不能访问非静态成员。
4.可以通过对象或类名(不需要实例化)访问静态成员。